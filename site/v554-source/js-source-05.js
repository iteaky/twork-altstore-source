window.__TWQ554_SOURCE=(window.__TWQ554_SOURCE||"")+"arding_data_import_yes_subtitle'),iconName:'import',action:'boolean',name:'wantsDataImport'})}${choiceCard({id:'false',selected:state.wantsDataImport===false,titleText:L('l10n.onboarding_data_import_no'),subtitleText:L('l10n.onboarding_data_import_no_subtitle'),iconName:'plus',action:'boolean',name:'wantsDataImport'})}</div>`;\n  }\n  function renderDone(){\n    return `${title('l10n.onboarding_done_title','l10n.onboarding_done_subtitle','done')}\n      ${bullets(['l10n.onboarding_done_bullet_settings','l10n.onboarding_done_bullet_client','l10n.onboarding_done_bullet_schedule'])}`;\n  }\n\n  const renderers={\n    welcome:renderWelcome,language:renderLanguage,clientLanguages:renderClientLanguages,theme:renderTheme,\n    workFormat:renderWorkFormat,workplace:renderWorkplace,clientCardIntro:renderClientCardIntro,trainingLifecycle:renderTrainingLifecycle,\n    timeZones:renderTimeZones,duration:renderDuration,workingHours:renderWorkingHours,subscriptions:renderSubscriptions,\n    subscriptionExpiration:renderSubscriptionExpiration,subscriptionDurationTemplates:renderSubscriptionTemplates,\n    subscriptionLifecycle:renderSubscriptionLifecycle,serviceLifecycle:renderServiceLifecycle,paymentControl:renderPaymentControl,\n    currencies:renderCurrencies,clubs:renderClubs,clubFeatures:renderClubFeatures,clubPayments:renderClubPayments,\n    clubOperations:renderClubOperations,clubSummary:renderClubSummary,trainingPricesQuestion:renderPricesQuestion,\n    priceLocationMode:renderPriceLocationMode,trainingPrices:renderTrainingPrices,groupPrices:renderGroupPrices,\n    subscriptionPricesQuestion:renderSubscriptionPricesQuestion,subscriptionPrices:renderSubscriptionPrices,\n    servicePrices:renderServicePrices,notifications:renderNotifications,notificationTypes:renderNotificationTypes,\n    notificationScheduleMode:renderNotificationScheduleMode,notificationScheduleDetails:renderNotificationScheduleDetails,\n    defaultMessenger:renderDefaultMessenger,calendarTypesIntro:renderCalendarTypesIntro,calendar:renderCalendar,\n    calendarNextSteps:renderCalendarNextSteps,backup:renderBackup,dataImport:renderDataImport,done:renderDone\n  };\n\n  function stepCanContinue(step=state.currentStep){\n    if(step==='clientLanguages')return state.clientLanguagesFollowAppLanguage||state.clientLanguages.size>0;\n    if(step==='workFormat')return state.workFormats.size>0;\n    if(step==='workplace')return !!state.workplace&&(!state.workFormats.has('group')||usesClubs());\n    if(step==='timeZones')return state.usesClientTimeZones!==null;\n    if(step==='workingHours')return state.wantsWorkingHours!==null&&(!state.wantsWorkingHours||state.workingDays.every(d=>!d.isWorking||d.end>d.start));\n    if(step==='subscriptions')return state.usesSubscriptions!==null;\n    if(step==='subscriptionDurationTemplates')return state.subscriptionDurationTemplates.every(t=>t.trainingCount>0&&t.validityMonths>0);\n    if(step==='paymentControl')return state.wantsPaymentControl!==null;\n    if(step==='currencies')return state.currencies.length>0;\n    if(step==='clubs')return state.clubs.length>0&&state.clubs.every(c=>c.name.trim());\n    if(step==='clubFeatures')return state.clubs.every(c=>c.worksWithClients||c.groupTrainingsEnabled||c.dutyEnabled||c.fixedPaymentEnabled)&&(!state.workFormats.has('group')||state.clubs.some(c=>c.groupTrainingsEnabled));\n    if(step==='clubPayments')return state.clubs.filter(c=>c.worksWithClients||c.groupTrainingsEnabled).every(c=>{\n      const modeValid=(mode,percent,fixed)=>mode==='NO_RETENTION'||(mode==='PERCENT_RETENTION'&&percent>=0&&percent<=100)||(mode==='FIXED_RETENTION'&&fixed>=0);\n      if(c.settlementRulesMode==='common')return modeValid(c.settlementMode,c.retentionPercent,c.fixedRetentionAmount);\n      return modeValid(c.personalSettlementMode,c.personalSingleRetentionPercent,c.personalSingleFixedRetentionAmount)\n        &&modeValid(c.groupSettlementMode,c.groupRetentionPercent,c.groupFixedRetentionAmount)\n        &&modeValid(c.serviceSettlementMode,c.serviceRetentionPercent,c.serviceFixedRetentionAmount);\n    });\n    if(step==='clubOperations')return state.clubs.every(c=>(!c.groupTrainingsEnabled||(c.groupTrainingParticipantPricingEnabled?c.groupParticipantPrice>0:c.groupTrainingPrice>0))&&(!c.dutyEnabled||c.dutyAmount>0)&&(!c.fixedPaymentEnabled||c.fixedPaymentAmount>0));\n    if(step==='trainingPricesQuestion')return state.wantsDefaultPrices!==null;\n    if(step==='priceLocationMode')return state.trainingPriceLocationModeAnswered;\n    if(step==='notifications')return state.wantsNotifications!==null;\n    if(step==='calendar')return state.wantsCalendarSync!==null&&(!state.wantsCalendarSync||state.trainerDisplayName.trim());\n    if(step==='dataImport')return state.wantsDataImport!==null;\n    return true;\n  }\n  function shouldShowSkipQuestion(step=state.currentStep){\n    return ['clientLanguages','timeZones','workingHours','subscriptions','paymentControl','trainingPricesQuestion','notifications','calendar','dataImport'].includes(step);\n  }\n  function primaryTitle(){\n    if(state.currentStep==='welcome')return L('l10n.start_setup');\n    if(state.currentStep==='done')return U('finish');\n    return L('l10n.continue');\n  }\n  function normalizeCurrentStep(){\n    const steps=currentSteps();\n    if(!steps.includes(state.currentStep)){\n      state.currentStep=steps[Math.min(currentIndex(),steps.length-1)]||'welcome';\n    }\n  }\n  function render(){\n    normalizeCurrentStep();\n    root.dataset.quizLanguage=state.language;\n    root.dataset.currentStep=state.currentStep;\n    root.dir=state.language==='ar'?'rtl':'ltr';\n    root.dataset.themeScheme=state.themeScheme;\n    root.dataset.themeStyle=state.themeStyle;\n    const steps=currentSteps();\n    const index=Math.max(0,steps.indexOf(state.currentStep));\n    const [groupKey,groupName]=currentGroup(state.currentStep);\n    root.dataset.group=groupName;\n    root.querySelector('.twq6-kicker').textContent=L(groupKey);\n    root.querySelector('.twq6-step-name').textContent=`${U('step')} ${index+1} ${U('of')} ${steps.length}`;\n    root.querySelector('.twq6-progress i').style.width=`${steps.length>1?(index/(steps.length-1))*100:100}%`;\n    root.querySelector('.twq6-skip').textContent=U('skipQuiz');\n    root.querySelector('.twq6-back').textContent=L('l10n.back');\n    root.querySelector('.twq6-back').disabled=index===0;\n    root.querySelector('.twq6-next').textContent=primaryTitle();\n    root.querySelector('.twq6-next').disabled=!stepCanContinue();\n    const skipButton=root.querySelector('.twq6-question-skip');\n    skipButton.textContent=U('skipQuestion');\n    skipButton.hidden=!shouldShowSkipQuestion();\n    const renderer=renderers[state.currentStep]||renderWelcome;\n    page.innerHTML=renderer();\n    const walker=document.createTreeWalker(page,NodeFilter.SHOW_TEXT);\n    while(walker.nextNode()){\n      const node=walker.currentNode;\n      if(/\\bl10n\\.[A-Za-z0-9_]+/.test(node.nodeValue||'')){\n        console.error('[TWORK quiz] Visible localization token:',node.nodeValue);\n        node.nodeValue=(node.nodeValue||'').replace(/l10n\\.([A-Za-z0-9_]+)/g,(_,key)=>key.replace(/_/g,' ').replace(/\\b\\w/g,m=>m.toUpperCase()));\n      }\n    }\n    scroll.scrollTop=0;\n  }\n\n  function setNested(path,value){\n    const parts=path.split('.');\n    let target=state;\n    for(let i=0;i<parts.length-1;i++){\n      const part=/^\\d+$/.test(parts[i])?Number(parts[i]):parts[i];\n      target=target[part];\n      if(target==null)return;\n    }\n    const last=/^\\d+$/.test(parts.at(-1))?Number(parts.at(-1)):parts.at(-1);\n    const current=target[last];\n    if(typeof current==='number')value=Number(value);\n    target[last]=value;\n    if(path==='trainingPricesDifferByLocation')state.trainingPriceLocationModeAnswered=true;\n  }\n  function getNested(path){\n    return path.split('.').reduce((acc,key)=>acc==null?undefined:acc[/^\\d+$/.test(key)?Number(key):key],state);\n  }\n  function skipQuestion(){\n    switch(state.currentStep){\n      case 'clientLanguages':state.clientLanguagesFollowAppLanguage=true;state.clientLanguages=setFrom([state.language]);break;\n      case 'timeZones':state.usesClientTimeZones=false;break;\n      case 'workingHours':state.wantsWorkingHours=false;break;\n      case 'subscriptions':state.usesSubscriptions=false;break;\n      case 'paymentControl':state.wantsPaymentControl=false;break;\n      case 'trainingPricesQuestion':state.wantsDefaultPrices=false;break;\n      case 'notifications':state.wantsNotifications=false;break;\n      case 'calendar':state.wantsCalendarSync=false;break;\n      case 'dataImport':state.wantsDataImport=false;break;\n    }\n    nextStep();\n  }\n  function nextStep(){\n    if(state.currentStep==='done'){download();return}\n    if(!stepCanContinue()){showToast(U('required'));return}\n    const steps=currentSteps(),index=steps.indexOf(state.currentStep);\n    state.currentStep=steps[Math.min(index+1,steps.length-1)];\n    render();\n  }\n  function previousStep(){\n    const steps=currentSteps(),index=steps.indexOf(state.currentStep);\n    if(index>0){state.currentStep=steps[index-1];render()}\n  }\n  function open(){\n    state=defaultState();\n    state.currentStep='welcome';\n    root.classList.add('is-open');\n    root.setAttribute('aria-hidden','false');\n    document.documentElement.classList.add('twq6-open','quiz-open');\n    document.body.classList.add('twq6-open','quiz-open');\n    render();\n    setTimeout(()=>root.querySelector('.twq6-next')?.focus(),30);\n    return true;\n  }\n  function close(){\n    root.classList.remove('is-open');\n    root.setAttribute('aria-hidden','true');\n    document.documentElement.classList.remove('twq6-open','quiz-open');\n    document.body.classList.remove('twq6-open','quiz-open');\n  }\n  function showToast(message){\n    toast.textContent=message;\n    toast.classList.add('show');\n    clearTimeout(showToast.timer);\n    showToast.timer=setTimeout(()=>toast.classList.remove('show'),2200);\n  }\n  function serializableState(){\n    return {\n      appLanguage:state.language,\n      clientLanguages:state.clientLanguagesFollowAppLanguage?[state.language]:Array.from(state.clientLanguages),\n      appearance:{scheme:state.themeScheme,style:state.themeStyle,appIcon:state.appIcon},\n      workFormats:Object.fromEntries(WORK_FORMATS.map(([id])=>[id,state.workFormats.has(id)])),\n      workplace:state.workplace,\n      useClientTimeZones:state.usesClientTimeZones,\n      defaultTrainingDurationMinutes:Number(state.trainingDurationMinutes),\n      workingHours:{enabled:state.wantsWorkingHours,days:state.workingDays},\n      subscriptions:{\n        enabled:state.usesSubscriptions,\n        expirationMode:state.subscriptionExpirationMode,\n        durationTemplates:state.subscriptionDurationTemplates.map(({id,...rest})=>rest),\n        pricingMode:state.subscriptionPricingMode,\n        trainingCount:Number(state.subscriptionTrainingCount)\n      },\n      paymentControlEnabled:state.wantsPaymentControl,\n      defaultCurrency:state.currencies[0]||null,\n      activeCurrencies:[...state.currencies],\n      clubs:state.clubs.map(c=>({...c,id:undefined})),\n      defaultPricesEnabled:state.wantsDefaultPrices,\n      prices:state.prices,\n      services:state.services.map(({id,...rest})=>rest),\n      notifications:{enabled:state.wantsNotifications,...state.notifications},\n      calendar:{enabled:state.wantsCalendarSync,trainerDisplayName:state.trainerDisplayName},\n      backup:{choice:state.backupChoice,folderName:state.backupFolderName},\n      dataImportEnabled:state.wantsDataImport\n    };\n  }\n  function config(){\n    const configuration=serializableState();\n    return {\n      format:'twork-onboarding',\n      schemaVersion:2,\n      createdAt:new Date().toISOString(),\n      source:'twork-landing-ios-v550',\n      iosReferenceVersion:'v550',\n      configuration,\n      // Compatibility aliases consumed by the previous landing importer.\n      appLanguage:configuration.appLanguage,\n      clientLanguages:configuration.clientLanguages,\n      defaultCurrency:configuration.defaultCurrency,\n      activeCurrencies:configuration.activeCurrencies,\n      defaultTrainingDurationMinutes:configuration.defaultTrainingDurationMinutes,\n      workFormats:configuration.workFormats,\n      features:{\n        subscriptions:state.usesSubscriptions===true,\n        services:state.workFormats.has('onlineSupport'),\n        paymentControl:state.wantsPaymentControl===true,\n        notifications:state.wantsNotifications===true,\n        workingHours:state.wantsWorkingHours===true\n      },\n      clubs:configuration.clubs\n    };\n  }\n  function download(){\n    const blob=new Blob([JSON.stringify(config(),null,2)],{type:'application/vnd.twork.setup+json'});\n    const url=URL.createObjectURL(blob);\n    const a=document.createElement('a');\n    a.href=url;\n    a.download=`TWORK-setup-${new Date().toISOString().slice(0,10)}.tworksetup`;\n    document.body.appendChild(a);\n    a.click();\n    a.remove();\n    setTimeout(()=>URL.revokeObjectURL(url),1000);\n    showToast(U('download'));\n  }\n\n  root.addEventListener('click',event=>{\n    const el=event.target.closest('[data-action]');\n    if(!el)return;\n    const action=el.dataset.action,value=el.dataset.value,name=el.dataset.name;\n    if(action==='close'){close();return}\n    if(action==='back'){previousStep();return}\n    if(action==='next'){nextStep();return}\n    if(action==='skip-question'){skipQuestion();return}\n    if(action==='language'){\n      state.language=value;\n      if(state.clientLanguagesFollowAppLanguage)state.clientLanguages=setFrom([value]);\n      if(!state.currencies.length){/* keep currency choice explicit like iOS */}\n      render();return;\n    }\n    if(action==='client-language-mode'){\n      state.clientLanguagesFollowAppLanguage=value==='same';\n      if(state.clientLanguagesFollowAppLanguage)state.clientLanguages=setFrom([state.language]);\n      render();return;\n    }\n    if(action==='toggle-client-language'){\n      state.clientLanguages.has(value)?state.clientLanguages.delete(value):state.clientLanguages.add(value);\n      render();return;\n    }\n    if(action==='theme-mode'){state.themeScheme=value;render();return}\n    if(action==='theme-style'){state.themeStyle=value;render();return}\n    if(action==='app-icon'){state.appIcon=value;render();return}\n    if(action==='toggle-work-format'){\n      state.workFormats.has(value)?state.workFormats.delete(value):state.workFormats.add(value);\n      render();return;\n    }\n    if(action==='workplace'){\n      state.workplace=value;\n      if(usesClubs())ensureClub();\n      render();return;\n    }\n    if(action==='boolean'){\n      setNested(name,value==='true');\n      if(name==='trainingPricesDifferByLocation')state.trainingPriceLocationModeAnswered=true;\n      render();return;\n    }\n    if(action==='duration'){state.trainingDurationMinutes=Number(value);render();return}\n    if(action==='disable-working-day'){state.workingDays[Number(name)].isWorking=false;render();return}\n    if(action==='enable-working-day'){state.workingDays[Number(value)].isWorking=true;render();return}\n    if(action==='add-working-break'){\n      const day=state.workingDays[Number(value)];\n      const start=day.start<'12:00'&&day.end>'13:00'?'12:00':day.start;\n      const end=day.start<'12:00'&&day.end>'13:00'?'13:00':day.end;\n      day.breaks.push({start,end});render();return;\n    }\n    if(action==='remove-working-break'){state.workingDays[Number(name)].breaks.splice(Number(value),1);render();return}\n    if(action==='toggle-currency-picker'){state.currencyPickerOpen=!state.currencyPickerOpen;state.currencySearch='';render();return}\n    if(action==='club-location'){state.clubs[Number(name)].locationType=value;render();return}\n    if(action==='calendar-guide-tab'){state.calendarGuideTab=value;render();return}\n    if(action==='subscription-expiration'){state.subscriptionExpirationMode=value;render();return}\n    if(action==='add-template'){\n      state.subscriptionDurationTemplates.push({id:uid(),trainingCount:Math.max(1,Number(state.newSubscriptionTrainingCount)||1),validityMonths:Math.max(1,Number(state.newSubscriptionMonths)||1)});\n      render();return;\n    }\n    if(action==='remove-template'){state.subscriptionDurationTemplates=state.subscriptionDurationTemplates.filter(x=>x.id!==value);render();return}\n    if(action==='add-currency'){state.currencies.push(value);state.currencyPickerOpen=false;state.currencySearch='';state.clubs.forEach(c=>{if(!c.currency)c.currency=value});render();return}\n    if(action==='remove-currency'){\n      state.currencies=state.currencies.filter(x=>x!==value);\n      if(state.currencies.length)state.clubs.forEach(c=>{if(c.currency===value)c.currency=state.currencies[0]});\n      render();return;\n    }\n    if(action==='default-currency'){state.currencies=[value,...state.currencies.filter(x=>x!==value)];render();return}\n    if(action==='add-club'){state.clubs.push(newClub(selectedCurrencies()[0]));render();return}\n    if(action==='remove-club'){state.clubs=state.clubs.filter(x=>x.id!==value);ensureClub();render();return}\n    if(action==='club-payment-default'){state.clubs[Number(name)].clientPaymentDefault=value;render();return}\n    if(action==='club-rules-mode'){\n      const c=state.clubs[Number(name)];c.settlementRulesMode=value;c.didConfigureSeparateSettlementRules=value==='separate';render();return;\n    }\n    if(action==='club-mode'){setNested(name,value);render();return}\n    if(action==='subscription-pricing-mode'){state.subscriptionPricingMode=value;render();return}\n    if(action==='add-service'){state.services.push({id:uid(),serviceTypeId:'ONLINE_ACCESS',durationMonths:1,priceByCurrency:{}});render();return}\n    if(action==='remove-service'){state.services=state.services.filter(x=>x.id!==value);if(!state.services.length)state.services.push({id:uid(),serviceTypeId:'ONLINE_ACCESS',durationMonths:1,priceByCurrency:{}});render();return}\n    if(action==='notification-schedule'){state.notifications.scheduleChoice=value;render();return}\n    if(action==='default-messenger'){state.notifications.defaultMessenger=value;render();return}\n    if(action==='";
